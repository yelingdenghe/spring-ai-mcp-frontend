# 简化版 Dockerfile - 适用于已有构建产物的场景
# 使用前需要先执行：npm run build

FROM nginx:latest

# 删除默认的 nginx 配置
RUN rm /etc/nginx/conf.d/default.conf

# 复制构建产物到 nginx 目录
COPY dist /usr/share/nginx/html

# 复制 nginx 配置文件
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]

